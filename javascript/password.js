function decrypt(num, str){
  alert(number+string)
  for(i=0; i < number; i++){
  }
}
function encrypt(num, str){
  alert(number+string)
  for(i=0; i < number; i++){
    var len = str.lenght;
    var newString = ""
    for(j=0; j < len; j++){
      chr.fromCharCode( str.charCodeAt(0)+1);
      newString = newString + chr
    }
  }
  return newString;
}
